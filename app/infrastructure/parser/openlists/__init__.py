"""
Движок «открытых списков»: разбор рейтинговых списков по форме, а не по вёрстке.

Пять вузов — ВШЭ, ИТМО, МГИМО, МГУ, РАНХиГС — публикуют списки каждый по-своему:
кто таблицей на странице, кто JSON-ответом внутреннего API. Писать пять
отдельных скребков по разметке значило бы чинить их по очереди после каждого
редизайна.

Вместо этого движок опирается на то, что у всех одинаково и закреплено
Порядком приёма: в списке обязаны быть уникальный код поступающего, сумма
конкурсных баллов, баллы за вступительные испытания и индивидуальные
достижения, приоритет и отметка о согласии. Мы ищем в ответе таблицу (или
массив объектов) с такими колонками — как бы ни назывались классы в вёрстке.

Что где лежит:
    tables.py   — HTML → таблицы (заголовки, строки, текст перед таблицей);
    columns.py  — заголовок колонки → наше поле, с синонимами всех пяти вузов;
    records.py  — таблица/JSON → SubmissionStats и Application;
    crawl.py    — HTTP и поиск ссылок на списки на странице-оглавлении;
    source.py   — сборка всего этого в IUniversitySource;
    specs.py    — по одному описанию источника на вуз.
"""
