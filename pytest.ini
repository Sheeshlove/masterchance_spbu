[pytest]
testpaths = tests
python_files = test_*.py
# Тесты офлайновые: сеть не нужна, рабочая data/master.db не трогается
# (DATABASE_URL подменяется в корневом conftest.py).
addopts = -q --strict-markers
