# Как поставить всё на сервер и обновлять данные без своего компьютера

Эта инструкция — про **Часть 2** из [КАК_ЗАПУСТИТЬ.md](КАК_ЗАПУСТИТЬ.md): как
сделать так, чтобы данные для приложения считались **сами, на отдельном
компьютере в интернете**, а ваш Mac был не нужен.

Написано максимально подробно. Если вы никогда не видели командную строку —
ничего страшного, просто копируйте команды по одной.

---

## Зачем вообще сервер

Чтобы приложение показывало шансы, кто-то должен:

1. скачать списки со всех направлений СПбГУ (их около 180);
2. прогнать **10 000 симуляций** конкурса;
3. выложить результат в интернет.

Это занимает десятки минут и требует, чтобы компьютер всё это время был
включён. Держать ради этого включённым свой Mac неудобно,
поэтому берём **сервер** — компьютер, который стоит в дата-центре и работает
круглосуточно. Настраиваем его один раз, дальше он всё делает сам по
расписанию.

> **Источник данных — СПбГУ.** Списки берутся из отчёта
> `enrollelists.spbu.ru`. Браузер для этого не нужен, поэтому сервер требуется
> совсем скромный.
>
> ℹ️ **Про даты экзаменов.** В отчёте СПбГУ есть только названия вступительных
> испытаний, без дат. Поэтому в приложении вместо расписания будет написано
> «Расписание экзамена пока не опубликовано». Баллы, шансы и проходные
> показываются как обычно.

---

## Что вам понадобится

- банковская карта (сервер стоит примерно 300–700 ₽ в месяц);
- ваш Mac — только чтобы подключаться к серверу;
- аккаунт GitHub (у вас уже есть).

---

## Шаг 1. Возьмите сервер

Подойдёт любой хостинг виртуальных серверов (VPS). Например Timeweb, Selectel,
Beget, aeza — порядок действий у всех одинаковый.

При заказе выберите:

| Параметр | Что выбрать | Почему |
| --- | --- | --- |
| Операционная система | **Ubuntu 24.04** (или 22.04) | под неё написана инструкция |
| Оперативная память | **2 ГБ** | хватает: браузер не нужен, тяжёлый только расчёт |
| Процессор | 2 ядра | симуляции считаются быстрее |
| Диск | **от 10 ГБ** | образ без браузера занимает около 200 МБ |

После оплаты хостинг пришлёт письмо или покажет на экране три вещи —
**запишите их**:

- **IP-адрес** сервера (например `95.163.180.22`);
- **имя пользователя** (обычно `root`);
- **пароль**.

---

## Шаг 2. Подключитесь к серверу со своего Mac

1. Нажмите **⌘ + пробел**, наберите **Терминал** и нажмите Enter. Откроется
   окно с текстом — это и есть командная строка.
2. Наберите команду, подставив свой IP вместо `ВАШ_IP`:

   ```bash
   ssh root@ВАШ_IP
   ```

3. При самом первом подключении спросит:
   `Are you sure you want to continue connecting?` — наберите **yes** и Enter.
4. Спросит пароль. Наберите его и нажмите Enter.
   **Пароль не отображается — ни точками, ни звёздочками.** Это нормально,
   просто печатайте вслепую.

Если в начале строки появилось что-то вроде `root@server:~#` — вы внутри
сервера. Дальше **все команды выполняются здесь**, в этом же окне.

> **Как вернуться позже?** Открыть Терминал и снова выполнить `ssh root@ВАШ_IP`.
> Чтобы выйти с сервера — команда `exit`.

---

## Шаг 3. Установите Docker

Docker — программа, которая ставит всё нужное (Python и библиотеки)
одним куском, чтобы вы не устанавливали это по частям.

Скопируйте и выполните **одну команду целиком**:

```bash
curl -fsSL https://get.docker.com | sh
```

Побегут строчки — это нормально, ждите. Проверьте, что получилось:

```bash
docker --version
```

Должно ответить что-то вроде `Docker version 27.3.1`. Если ответило — идём
дальше.

---

## Шаг 4. Скачайте проект на сервер

```bash
apt update && apt install -y git
git clone https://github.com/Sheeshlove/masterchance_spbu.git
cd masterchance_spbu
```

Последняя команда «входит» в папку проекта. Проверить, что вы там:

```bash
ls
```

Должны увидеть список файлов: `bot.py`, `desktop.py`, `README.md` и другие.

---

## Шаг 5. Создайте файл с настройками

Выполните команду целиком — она создаст файл `.env` со всеми настройками:

```bash
cat > .env <<'EOF'
ENV=prod
TIMEZONE=Europe/Moscow
DATA_DIR=/app/data
DB_FILENAME=master.db

# Источник данных (сейчас поддерживается только СПбГУ)
UNIVERSITIES=spbgu

# Сколько списков тянуть одновременно. Это обычные запросы, не браузеры,
# так что 4 — вежливо к серверу вуза и достаточно быстро.
PARSER_PARALLELISM=4

# Токен для публикации данных — вставите на шаге 7
GITHUB_TOKEN=
EOF
```

Проверьте, что файл создался:

```bash
cat .env
```

> Токен пока пустой — это правильно, заполним его чуть позже.

---

## Шаг 6. Соберите программу на сервере

```bash
docker build -t masterchance:local .
```

Это самая долгая установка — **5–15 минут**. Побегут длинные строчки, в конце
должно появиться что-то со словами `naming to docker.io/library/masterchance:local`.

Проверка:

```bash
docker images | grep masterchance
```

Если строчка появилась — образ собран.

---

## Шаг 7. Получите ключ для публикации данных (токен GitHub)

Чтобы сервер мог сам выкладывать готовые данные на GitHub, ему нужен «пропуск».
Это делается **в браузере на вашем Mac**:

1. Откройте https://github.com/settings/tokens
2. Нажмите **`Generate new token`** → выберите **`Fine-grained token`**.
3. Заполните:
   - **Token name** — любое, например `masterchance-server`;
   - **Expiration** — срок жизни. Поставьте побольше (например 1 год), иначе
     через месяц всё перестанет публиковаться;
   - **Repository access** → **`Only select repositories`** → выберите
     **`masterchance_spbu`**;
   - **Permissions** → **`Repository permissions`** → найдите строку
     **`Contents`** и поставьте ей **`Read and write`**.
4. Внизу нажмите **`Generate token`**.
5. Появится длинная строка вида `github_pat_11ABC...`. **Скопируйте её сейчас** —
   второй раз GitHub её не покажет.

Теперь вернитесь в Терминал (вы на сервере) и впишите токен в настройки:

```bash
nano .env
```

Откроется простой редактор. Стрелками спуститесь к строке `GITHUB_TOKEN=` и
вставьте скопированный токен сразу после знака `=`, чтобы получилось:

```
GITHUB_TOKEN=github_pat_11ABC...
```

Сохраните: нажмите **Control + O**, затем **Enter**, затем **Control + X**.

---

## Шаг 8. Запустите — одной командой

Эта команда делает всё: **сразу** начинает первый сбор, а дальше сама обновляет
данные **каждые 3 часа**. Терминал после неё можно закрыть, Mac — выключить.

```bash
docker run -d --name masterchance-updater --restart unless-stopped \
    --env-file .env \
    -v "$PWD/data:/app/data" -v "$PWD/dist:/app/dist" \
    masterchance:local scripts/autoupdate.py
```

Команда сразу вернёт вас обратно в строку и напечатает длинный код — это
нормально, так и должно быть. Работа идёт в фоне.

**Что значат части команды** (на случай, если интересно):

| Часть | Зачем |
| --- | --- |
| `-d` | «в фоне» — не держит терминал |
| `--restart unless-stopped` | сам поднимется после перезагрузки сервера |
| `--name masterchance-updater` | имя, по которому к нему обращаться |
| `-v "$PWD/data:/app/data"` | база остаётся на сервере, а не внутри контейнера |

### Посмотреть, что происходит

```bash
docker logs -f masterchance-updater
```

Побегут строчки со временем: `Шаг 1/4: списки и каталог…` и так далее.
**Первый сбор идёт долго — обычно десятки минут**: сервер обходит около 180
списков, потом считает 10 000 симуляций.

Чтобы выйти из просмотра, нажмите **Control + C** — обновление при этом **не
остановится**, вы просто перестанете смотреть в лог.

В конце прохода появится:

```
[2026-08-06 23:05:47] Проход завершён успешно.
[2026-08-06 23:05:47] Следующее обновление примерно в 02:05 07.08.
```

### Проверить, что живёт

```bash
docker ps
```

В списке должна быть строка `masterchance-updater` со статусом `Up`.

### Остановить, включить снова, удалить

```bash
docker stop masterchance-updater      # остановить
docker start masterchance-updater     # включить снова
docker rm -f masterchance-updater     # удалить совсем
```

> **Как изменить частоту?** Добавьте в `.env` строку `UPDATE_INTERVAL_HOURS=6`,
> затем пересоздайте контейнер: `docker rm -f masterchance-updater` и снова
> команда запуска. Чаще, чем раз в 10 минут, обновляться не станет — это защита
> сервера вуза от лишней нагрузки.

> **Если один сбор сорвался** (сайт вуза лежал, интернет моргнул) — цикл не
> умирает: ошибка попадёт в лог, а через 3 часа он попробует снова. Прежние
> данные при этом остаются на месте.

> **Нужен разовый запуск без фона?** `scripts/server_update.sh` делает один
> проход и завершается.

---

## Шаг 9. Проверьте, что данные появились

Откройте в браузере на Mac:

```
https://github.com/Sheeshlove/masterchance_spbu/releases
```

Должен появиться релиз **«Данные для приложения»** с файлом
`master-snapshot.db.gz`. Если он там — **всё работает**: теперь приложение на
любом компьютере найдёт эти данные само.

Проверьте наверняка — запустите `MasterChance` на своём Mac. В серой строчке
должно появиться «Данные загружены: …», а по вашему коду — проценты.

---

## Шаг 10 (необязательный). Сайт внутри Telegram — Mini App

Тот же сайт можно открывать прямо в Telegram, не выходя в браузер. Отдельного
приложения писать не нужно — это та же страница, она сама подстраивается.

**Одно жёсткое требование: нужен домен и HTTPS.** Telegram открывает Mini App
только по `https://` с настоящим сертификатом. По `http://` и по голому IP —
не откроет, молча. Если домена нет, этот шаг делать пока нечего.

### 10.1. Домен и сертификат

Купите домен (любой регистратор) и направьте его A-записью на IP сервера.
Дальше на сервере:

```bash
apt-get update && apt-get install -y nginx certbot python3-certbot-nginx
certbot --nginx -d ваш-домен.ru
```

Certbot сам выпустит сертификат и пропишет его в nginx. Осталось отправить
трафик на контейнер с сайтом — в `/etc/nginx/sites-available/default`, внутри
блока `server { ... }` с вашим доменом:

```nginx
location / {
    proxy_pass http://127.0.0.1:8080;
    proxy_set_header Host $host;
    proxy_set_header X-Forwarded-Proto $scheme;
}
```

```bash
nginx -t && systemctl reload nginx
```

Проверьте: `https://ваш-домен.ru` должен открыться в браузере с замочком.

### 10.2. Скажите проекту адрес

В `.env` на сервере добавьте строку:

```
WEBAPP_URL=https://ваш-домен.ru
```

Пересоберите образ и перезапустите бота и сайт (см. раздел «Как обновить
проект на сервере»). При старте бот сам пропишет кнопку меню — в логах
появится «Mini App подключён».

Если адрес не задан или начинается с `http://`, бот просто работает как
раньше, без кнопки. Это не ошибка.

### 10.3. Пропишите Mini App у @BotFather

1. Откройте [@BotFather](https://t.me/BotFather) → `/mybots` → ваш бот.
2. **Bot Settings → Menu Button → Configure menu button** → пришлите
   `https://ваш-домен.ru` и название кнопки, например `Открыть приложение`.

Готово: в чате с ботом слева от поля ввода появится кнопка, а `/start` пришлёт
ещё и кнопку под сообщением.

> **Про токен.** Он лежит только в `.env` на сервере. Не вставляйте его в
> код, в README, в переписку и в скриншоты: кто знает токен — тот полностью
> управляет ботом. Если токен всё же где-то засветился, немедленно смените
> его: @BotFather → `/mybots` → ваш бот → **API Token → Revoke current token**.
> Старый сразу перестанет работать, новый впишите в `.env` и перезапустите.

---

## Как обновить проект на сервере

Код в репозитории меняется. Сервер сам за ним не следит — он работает с той
версией, которую вы скачали и «запекли» в образ на шагах 4 и 6. Поэтому после
любых изменений в проекте выполните на сервере **две команды**:

```bash
cd ~/masterchance_spbu
git pull && docker build -t masterchance:local .
```

Это безопасно: ваш файл настроек `.env`, база в `data/` и собранные снапшоты в
`dist/` не отслеживаются гитом, поэтому `git pull` их не тронет.

> Забыть про этот шаг — самая частая причина ошибок вида
> `python: can't open file '/app/что-то.py'`: файл уже есть в репозитории, но
> ещё не попал в образ.

---

## Что делать, если что-то пошло не так

| Что вы видите | Что делать |
| --- | --- |
| `Permission denied (publickey,password)` при `ssh` | Неверный пароль или пользователь. Возьмите их из письма хостинга. |
| `docker: command not found` | Не выполнен шаг 3. |
| `python: can't open file '/app/…py'` | Образ собран из старой версии кода. Обновите: `git pull && docker build -t masterchance:local .` (см. раздел выше). |
| `ModuleNotFoundError: No module named '…'` | Не хватает библиотеки в образе. Обновите проект и пересоберите: `git pull && docker build -t masterchance:local .` |
| `Cannot connect to the Docker daemon` | Докер не запущен: `systemctl start docker` |
| Сборка образа падает на `pip install` | Обычно нехватка памяти. Возьмите тариф с 4 ГБ или добавьте подкачку: `fallocate -l 2G /swapfile && chmod 600 /swapfile && mkswap /swapfile && swapon /swapfile` |
| Шаг 1 падает, в логе `Killed` или `MemoryError` | Не хватило памяти на расчёт. Уменьшите в `.env`: `PARSER_PARALLELISM=2` |
| `❌ Не задан GITHUB_TOKEN` | Токен не подставился. Выполните `set -a && . ./.env && set +a` перед запуском (в cron это уже есть). |
| `❌ Не удалось создать релиз` | У токена нет права **Contents: Read and write**, либо истёк срок. Сделайте новый (шаг 7). |
| Данные собрались, но файла в Releases нет | Значит публикация пропущена из-за пустого токена — в логе будет строка «Шаг 4/4 пропущен». |
| Приложение отвечает «Заявок не найдено» на любой код | В базе нет заявок: источник вернул пустые списки. Запустите диагностику: `docker run --rm --env-file .env -v "$PWD/data:/app/data" masterchance:local scripts/diagnose_spbgu.py` |
| `Ни по одной программе не получено заявок` | Защита сработала: старые данные сохранены, обновление отменено. Причину покажет та же диагностика. |
| Приложение говорит «Не удалось скачать данные» | Проверьте, что файл виден по ссылке из шага 9 и репозиторий публичный. |
| Закончилось место на диске | `docker system prune -a` — удалит старые образы. |

---

## Коротко: что происходит после настройки

```
    СЕРВЕР (сам, каждые 3 часа)               АБИТУРИЕНТ
    ───────────────────────────               ──────────
    update_lists.py                           открывает MasterChance
      ├ скачивает списки вуза                 вводит свой код
      └ считает 10 000 симуляций                    │
    build_snapshot.py                               │
      └ упаковывает результат                       │
    publish_snapshot.sh                             ▼
      └ кладёт файл в GitHub  ──────────────►  видит свежие проценты
```

Ваш компьютер в этой схеме больше не участвует.
